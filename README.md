# To connect to lightsail instance

run cmd\
ssh my-lightsail\

# To update git repo

cd /srv/deploy\
git pull

# To run containers locally

cd infra/dev\
docker compose up -d

# To run dev on Next.js locally

cd web\
yarn dev
